# ----------------------------------------------------------------------
# HTTP to HTTPS REDIRECT
# ----------------------------------------------------------------------
server {
    listen 80;
    listen [::]:80;
    server_name proxy.watchthat.site;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# ----------------------------------------------------------------------
# HTTPS CONFIGURATION
# ----------------------------------------------------------------------
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name proxy.watchthat.site;

    ssl_certificate     /etc/letsencrypt/live/proxy.watchthat.site/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/proxy.watchthat.site/privkey.pem; # managed by Certbot

    location / {
        proxy_pass http://127.0.0.1:5200;
        include /etc/nginx/snippets/proxy-headers.conf;
    }
}
